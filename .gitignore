# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# OS junk
Thumbs.db
ehthumbs.db

# Node (in case anything is added later)
node_modules/
package-lock.json

# Local environment
.env
.env.local

# Build output (currently no build step — placeholder for future)
dist/
build/

# Per-deploy identity stamp — generated by scripts/gen-build-info.mjs at
# deploy time (and locally when testing). Never committed.
build-info.json

# Local Netlify CLI state (not for the repo / public site)
.netlify/

# Private research & strategy docs — must NOT be served publicly (publish=".")
co-work/

# Stray editor workspace files from other projects
*.code-workspace

# Supabase CLI local link state
supabase/.temp/
