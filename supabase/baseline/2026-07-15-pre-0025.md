# Schema Baseline — pre-`0025` (2026-07-15)

Task A1. A rollback reference and drift reconciliation captured **before** any Phase 0 DDL is applied. Live schema is authoritative (the migration-history table is unreliable — see below).

## Fingerprint (assert unchanged before applying `0025`)

| Metric | Value |
|---|---|
| `public` RLS policies | **50** |
| SECURITY DEFINER functions | **10** |
| non-internal triggers | **13** |
| `public` tables | **33** |

Re-run to confirm no drift immediately before `0025`:
```sql
select
 (select count(*) from pg_policies where schemaname='public'),
 (select count(*) from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and p.prosecdef),
 (select count(*) from pg_trigger where not tgisinternal),
 (select count(*) from pg_tables where schemaname='public');
```

## Migration-history reconciliation

- Repo `supabase/migrations/` holds **0001–0023**.
- The DB `supabase_migrations` history table is **missing 0010–0020** (their objects are live — applied out-of-band) and **includes `0024_children_mobility_profile_jsonb`, which has no repo file**.
- **Conclusion:** trust the live schema, not `list_migrations`. **Next migration number = `0025`.** Diff live `pg_policies`/`pg_proc` before and after applying it.

## Critical objects `0025` will change (exact current definitions — rollback reference)

Confirmed live 2026-07-15. Each is a RED hole; the harness (`../tests/security/`) asserts them.

| Object | Current definition (to restore on rollback) |
|---|---|
| `family_members.fm_insert` | `WITH CHECK ((user_id = auth.uid()) OR is_family_member(family_id))` |
| `family_members.fm_update` | `USING is_family_member(family_id)` · `WITH CHECK NULL` |
| `family_profiles.family_profiles_rw` | `ALL` · `USING/CHECK is_family_member(family_id)` |
| `children.child_upd` | `USING/CHECK (is_family_owner(family_id) OR self_user_id = auth.uid())` |
| `member_child_access.mca_write` | `ALL` · `USING/CHECK is_family_owner(family_id)` (no child↔family binding) |
| `families.fam_update` | `USING (is_family_member(id) AND family_role_of(id) IN (architect,co_architect))` · `WITH CHECK NULL` |
| `child_profile_limit` grant | `UPDATE` granted to `authenticated` **and** `anon` |
| `can_access_child()` | MCA branch omits `a.family_id` (cross-family) |
| `apply_billing_entitlement()` | active branch `child_profile_limit = 1 + coalesce(new.extra_seats,0)` (no `greatest`) |
| `enforce_child_profile_limit` trigger | BEFORE **INSERT** only on `children` |

Full verbatim policy/function/trigger/grant DDL: `docs/phase0-security-remediation.md` §2 and the audit capture in this session.

## Data backup — ⚠️ owner action required before A3

I cannot trigger a Supabase point-in-time / physical backup from here. **Before `0025` is applied, confirm a restore point exists** (Supabase dashboard → Database → Backups / PITR). `0025` is additive and reversible (policy/trigger/grant changes, no destructive DDL), but a restore point is the required safety net for any production DDL.
